# Firebase App Hosting Configuration (Prototype Workspace)
scripts:
  buildCommand: npm run build
  runCommand: npm start

outputFiles:
  serverApp:
    include:
      - dist
      - server.js
      - package.json
